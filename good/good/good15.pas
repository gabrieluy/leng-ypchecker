program good15;

var
	x, y, z : integer;
	result : real;
	aux : bool;

function random(var m : integer; n : integer) : real;
begin
	m := m * 2 + 1024;
	random := (m + n) / z;
end;

begin
	while (not(false) < true) do
	begin
		if (aux) then result := random(x, 42)
		else result := random(x, y);
	end;
end.
